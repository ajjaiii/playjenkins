pipeline {
    agent any
    stages {
        stage('Checkout Source') {
            steps {
                   git url: ''
                   sh 'ls -al'
                   
            }
        }
     }
}
